"""
solver — Route optimization logic.

Contains the algorithm that turns a set of deliveries into optimized routes:
constraints, objective (e.g. minimize distance or time), and the actual
optimization step. Depends on models for input/output types and may use
utils for distance or time calculations.
"""
